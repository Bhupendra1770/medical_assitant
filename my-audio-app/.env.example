# WebSocket server URL (voice)
VITE_WS_URL=ws://localhost:8000

# HTTP API URL (text chat + document upload)
VITE_API_URL=http://localhost:8080

# Must match CHAT_API_KEY in backend .env
VITE_API_KEY=dev-key
